/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/UFC-GOAT-SIMULATOR',
  trailingSlash: true,
  images: { unoptimized: true },
};
module.exports = nextConfig;
